# v0.1.1 acceptance record

Owner QA + release disposition, 2026-07-22. Format follows
`v0.1.0`'s acceptance record: what was verified live, what was deliberately
skipped, and the owner decision that cut the release.

## Scope of the release

v0.1.1 shipped 21 closed issues across: the review-gate + ship-process
infrastructure (#122, #126, #128, #129, #131), wave 1 (#17 effect sizes,
#105 venv setup, #106 caffeine chart labels, #108 lux presets, #112
dashboard copy), wave 2 (#36 nav redesign, #134 evening clock, #113 monthly
night detail, #37/#38 stats renames), same-day owner-decided fixes (#97,
#98, #99, #100, #101, #102, #104 earlier in the milestone; #142 wrap window,
#143 rating display, #147 contrast durations on release day).

**Descoped to v0.1.2 by the release decision:** #107 (sick-day copy), #109
(unsaved-changes warning), #110 (supplement dose copy), #117
(caffeine-at-bedtime predictor).

## Live QA (owner, on his machine, dev build post wave 2)

1. **Nav redesign (#36/#138)** — ✅ "yes all good": labels, lightbulb Coach,
   gear Settings, curved active indicator, title→Dashboard, <640px icon-only
   collapse, keyboard activation.
2. **Analysis effect sentences (#17/#132)** — ✅ "looks good". Observations
   from this pass:
   - Caffeine rows showed no correlations — expected, not a defect: the
     engine requires ≥14 days with both the predictor and outcome recorded
     (`min_days=14`) plus variance in both columns (ADR 003: analysis only
     uses explicitly recorded days).
   - Minute-unit bin averages read as raw minutes ("avg 446.9") — owner
     directed the duration idiom instead; fixed same-day as #147 ("7h 27m",
     shipped in this release).
3. **Evening-clock normalization (#134/#142)** — ⏭️ deliberately skipped by
   the owner ("don't feel like testing this, maybe someday in the future").
   Risk accepted: behavior is pinned by regression tests (per-column
   after-midnight semantics, the 4 AM boundary, an end-to-end correlation
   sign-flip, and the early-riser-loses-to-afternoon aggregation case).

Items not exercised live this round (all battery-reviewed, 4 legs each, and
covered by automated tests; verify during ongoing dogfood): dashboard pill
wording/wrapping (#37/#141), monthly best/worst night line (#113/#139),
export Rating words (#143/#146), onboarding/settings target hints
(#38/#141), caffeine chart labels (#106/#135), weekly Top Factors caption
(#102/#125), clean `make setup` on macOS (#105/#133).

## Release disposition

Owner decision, 2026-07-22: "after this [the #147 contrast-duration fix]
tag for milestone" — release cut with the four remaining milestone items
moved to v0.1.2 rather than shipped first (the recommended option of the
three presented). Release mechanics per the ship skill and repo bindings:
dev→main PR with the full 4-leg review battery (standing owner decision
from #128's design conversation), merge commit, `v0.1.1` tag on main.

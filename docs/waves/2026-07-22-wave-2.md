# Wave 2 — v0.1.1 (2026-07-22)

Preflight: unchanged from wave 1 (verified live 2026-07-21/22): base dev /
release main; GitHub tracker writable; 6 required checks incl. review-gate,
strict up-to-date, PR required; gate enforced; squash merges; worktrees.
Fail-closed does not apply.

## Lanes (pairwise disjoint; disjoint from nothing in flight — wave 1 fully landed)
**Lane 1 — #134 evening-clock normalization (fix/134-evening-clock):**
normalize last_caffeine_hour/last_meal_hour/stimulating_last_hour onto the
24+ evening scale in _aggregate_daily_log (<6 → +24, same as bedtime);
remove the #132 display suppression (_UNNORMALIZED_CLOCK) and restore the
three to _HOUR_PREDICTORS/_EFFECT_INCREMENTS; after-midnight tests per
column; analysis-explainer line noting corrected numbers (#92 spirit).
Touch-set: backend/services/stats_engine.py, backend/tests/test_stats_engine.py,
frontend/src/components/Analysis/Explainer.tsx (one sentence).
**Lane 2 — #36 nav rebuild (feat/36-nav):** build the decided design record
(artifact 1b0e8517): labeled icons Log/Dashboard/Analysis/Coach/Reports/
Settings, pencil Log first, lightbulb Coach (replaces star), gear Settings
(replaces sun), curved line under active icon, title → Dashboard, tooltips+
aria throughout. Owner confirmed the preview 2026-07-22 ("greatly improved,
good to go"). Touch-set: frontend/src/components/Layout/Layout.tsx (+css),
Layout tests, e2e nav assertions if any pin icons/title behavior.
**Lane 3 — #37+#38 stats renames (fix/37-38-renames):** display-only:
σ→"Variability", δ→"Bedtime offset", Δ→"Weekend drift" everywhere (dashboard
ConsistencyMeter incl. dropping the redundant hover-tooltips in favor of the
expander, weekly-report consistency card, analysis predictor labels via
VARIABLE_LABELS display strings ONLY if frontend-side); "Typical Bedtime"→
"Target Bedtime" display labels (internal field names unchanged), both
onboarding sleep-profile fields marked optional + one line explaining why
targets are asked (decisions on #37/#38 threads). Touch-set: frontend
ConsistencyMeter.tsx, SleepProfileStep.tsx, ProfileSection.tsx, weekly
consistency card component, related tests. NO backend field renames.
**Lane 4 — #113 monthly best/worst detail (feat/113-monthly-nights):**
best/worst night cards gain weekday name, bedtime/sleep start, duration,
deep/REM/HRV alongside existing factor tags. Touch-set: backend/services/
report_service.py (monthly section + schemas NightSummary additive fields),
backend/tests/test_report_service.py, frontend MonthlyReportView.tsx (+test),
frontend/src/types/reports.ts.

## Deferred to wave 3 (dependencies)
#117 stacks on Lane 1 (same stats_engine surface; + one-day lookback +
no-caffeine-day per issue thread). #107+#109+#110 share DailyLogPage → stack.

## Escalation check
All four lanes implement explicit owner decisions (bug #134 = bug-fix class;
#36 design record owner-confirmed; #37/#38 decided on threads; #113
owner-requested detail). No open decisions. Wave-1 report committed copy
rides Lane 1 (first expected merge) per the Phase 5 ordering owed from wave 1.

# Single-change lane: #17 effect sizes (2026-07-22)

## Preflight (verified live 2026-07-21/22)
- Base branch `dev`, release branch `main`.
- Tracker: GitHub issues/milestones (write access confirmed; issues close manually at merge).
- CI: 6 required checks on dev+main — backend (3.11), backend (3.12), frontend, security, e2e, review-gate; ruleset 18500043, PR required, strict up-to-date on; merge = squash.
- Review gate: live (`scripts/review_gate.py`, `docs/process/review-gate.md`), required check, verified blocking on probe PR #130.
- Merge permissions: yes (non-admin token; admin bypass is human-only by policy).
- Worktrees: supported. Single lane — main checkout serves as the lane; review legs run against origin refs.
- Fail-closed condition: does not apply.

## Lane: #17 — correlations become effect sizes
**Decided format (owner, on the issue):** slope headline in natural units,
median-split binned contrast as evidence, raw r demoted to secondary.

**Scope:** Analysis ranked list (built) + report Top Factors card (this
lane). Recommendation-body effect phrasing is deliberately deferred to the
dashboard-coach work (#114), where rec content is being redesigned anyway —
recorded on #17 at close.

**Approach:** backend `_effect_size`/`_binned_contrast` layer in
stats_engine attached to every correlation result (done, tested);
`effectFormat.ts` renders the headline/evidence strings; CorrelationList
leads with them (done); `_get_top_factors` passes each factor's effect
through the schema so TopFactorsCard shows the slope phrase beside r.

**Predicted touch-set:**
- backend/services/stats_engine.py (done)
- backend/services/report_service.py (top-factors effect pass-through)
- backend/schemas.py (EffectSize/BinnedContrast models — done; TopFactor.effect)
- backend/tests/test_stats_engine.py (done), backend/tests/test_report_service.py
- frontend/src/effectFormat.ts (done), frontend/src/types/analysis.ts (done),
  frontend/src/types/reports.ts, frontend/src/components/Analysis/CorrelationList.tsx (done),
  frontend/src/components/Analysis/AnalysisPage.css (done),
  frontend/src/components/Reports/TopFactorsCard.tsx (+ test),
  frontend/src/components/Analysis/AnalysisPage.test.tsx,
  frontend/src/components/Analysis/CorrelationHeatmap.test.tsx (mock shape — done)
- docs/reviews/pr-<N>/ (gate artifacts), this plan file

**Escalation check:** the display-format change is user-visible, and the
format was explicitly decided by the owner on the issue thread (slope +
binned, options 1+2) — no open decision. Autonomous merge intended.

## Touch-set gate result (pre-PR)
Actual diff = predicted set plus one overflow:
`frontend/src/components/Reports/ReportsPage.test.tsx` — its WeeklyReport
mock needed the new required `effect` field on TopFactor. Mechanical
consequence of the schema change, no scope growth. Accepted and recorded.

## Review round scope changes (PR #132)
- **HTML export parity ADDED** (was deferred in the plan): weekly HTML
  export factor items now carry the same compact slope phrase the SPA's
  TopFactorsCard shows (`render_weekly_html` in report_service.py), with the
  same 0.05 display floor and the module's T-04 escape invariant kept.
- **Evening-clock columns display-suppressed pending #134**:
  `last_caffeine_hour`, `last_meal_hour`, `stimulating_last_hour` are raw
  0-24 clock columns (unlike bedtime_hour's 24+ evening scale), so "per hour
  later" slopes and before/after bins are false for after-midnight events.
  Effect and contrast are suppressed for these predictors until #134
  normalizes the columns onto the evening scale.

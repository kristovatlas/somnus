# Wave 1 — v0.1.1 papercuts (2026-07-22)

Preflight (verified live 2026-07-21/22): base  / release ;
GitHub tracker writable; 6 required checks (backend 3.11/3.12, frontend,
security, e2e, review-gate) on dev+main, strict up-to-date, PR required;
review gate enforced (probe-verified); squash merges; worktrees supported.
Fail-closed condition does not apply.

## Independence analysis of the remaining milestone
In flight: #17 (PR #132) touches stats_engine, report_service, schemas,
Analysis/*, Reports/*, effectFormat, types → anything overlapping waits.

- **Wave 1 lanes (disjoint from each other and from #132):**
  #105 (Makefile+README venv), #106 (CaffeineChart labels),
  #108 (SunlightSection lux presets), #112 (Dashboard copy).
- **Wave 2 (blocked on #132 merge or on decisions):** #37+#38 (renames — touch
  Reports/ConsistencyMeter surfaces #132 also edits; also one stacked pair,
  same copy), #113 (report_service/Reports), #117 (stats_engine + backend
  caffeine model port; big — possibly its own single), #107+#109 (share
  DailyLogPage → stack), #110 (SupplementSection + DailyLogPage → stack with
  #107/#109), #36 (open product decision, see preflight question).
- Shared docs: no lane touches CLAUDE.md/THREAT_MODEL/acceptance checklist.
  This plan file rides the first wave-1 lane to merge.

## Lanes
**Lane A — #105 make setup venv (fix/105-macos-venv):** `make setup` creates/
uses `.venv` when no virtualenv is active; README documents it. Touch-set:
Makefile, README.md.
**Lane B — #106 caffeine chart labels (feat/106-caffeine-chart-labels):**
label the bedtime marker ("Bedtime 11:30 PM"), add "≈N mg at bedtime"
callout, label y-axis zero; rename "+ Add custom entry" → "+ Add entry".
Touch-set: frontend CaffeineChart/* (+ caffeineCalc if needed), DailyLog
CaffeineSection.tsx (button label), tests.
**Lane C — #108 sunlight lux presets (feat/108-lux-presets):** preset chips
(heavy overcast 1k / overcast 5k / partly cloudy 20k / full sun 80k / indoor
window 1k) writing into the existing lux field; manual entry unchanged.
Touch-set: frontend DailyLog SunlightSection.tsx (+ css/test).
**Lane D — #112 dashboard copy (fix/112-dashboard-copy):** "You're in your
optimal bedtime window"; red-light card gains "Last 7 days" qualifier and
"1 more session this week to stay at 3/week" phrasing. Touch-set: frontend
bedtime.ts, BedtimeCountdown.tsx, RedLightSummary.tsx, tests.

**Escalation check:** #105 bug-fix class; #106/#108/#112 implement wording/
presets the owner specified in his dogfood journal and the issues record —
no open decisions. #36 excluded pending the preflight question.

## Decision preflight (batched, answer anytime — no lane blocked)
**#36 nav:** the remedy involves choices the issues don't settle: add a
sixth nav icon for the Daily Log? repoint the "Somnus" title to the
Dashboard (journal expectation) with the log reachable only via the new
icon? Recommendation: title → Dashboard, new calendar/pencil icon → Daily
Log, tooltips on all. Wave 2 builds whatever is decided.

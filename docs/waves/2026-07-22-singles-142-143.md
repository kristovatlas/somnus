# Singles — 2026-07-22 (post wave 2)

Two independent owner-decided fixes from the wave-2 report's decision list,
run as parallel single-change lanes per the ship skill's single-change mode
(one-lane plans, predicted touch-sets, full battery + Phase 4 gate each).
Both decisions were made explicitly by the owner in conversation on
2026-07-22, so neither carries an open escalation item.

**Lane A — #142 consumption wrap window (fix/142-consumption-wrap-window):**
Owner decision: "4 am sounds reasonable." Narrow `_evening_time_to_hour`'s
wrap cutoff from `< 6` to `< 4` for last-caffeine/meal/stimulating (bedtime
keeps `< 6` via `_normalize_bedtime_hour`, untouched). Update the docstring
(cutoffs now deliberately differ; cite the owner decision and #144 for the
travel-robust future), the `_HOUR_PREDICTORS` comment, the boundary test
(3:59/4:00 replaces 5:59/6:00), and tighten the Analysis explainer's
"after midnight" sentence to "(before 4 AM)". Owner also directed
(same conversation): a more robust approach for travelers later — filed as
#144; the 4 AM heuristic is explicitly "good enough for now".
Touch-set: `backend/services/stats_engine.py`,
`backend/tests/test_stats_engine.py`,
`frontend/src/components/Analysis/Explainer.tsx` (+ its test if copy is
pinned). Closes #142.

**Lane B — #143 export rating display (fix/143-rating-display):**
Owner decision: "off target is good." Display-map rating enum values at the
HTML-export sink only (the SPA uses ratings solely for pill color — verified
no other surface renders the words): generic underscores→spaces plus
`drifting` → "off target", so "drift" stays reserved for Weekend Drift.
Band *values* (reference_data, API schema, dashboard payload) unchanged —
they are shared vocabulary. Tests pin the three mapped words and assert no
raw `on_target`/`drifting`/underscore ratings in the export.
Touch-set: `backend/services/report_service.py`,
`backend/tests/test_report_service.py`. Closes #143.

Touch-sets are pairwise disjoint (checked at file level — the wave-2 lesson).
Merge serially as batteries clear; post-merge green between merges.

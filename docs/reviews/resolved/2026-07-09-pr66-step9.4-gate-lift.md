# Review — PR #66: Step 9.4 lift the threat-model gate; wire the standing rule

- **Status:** RESOLVED 2026-07-09 — all 3 findings fixed on the PR branch
- **Resolution:** #1 (phantom eslint-plugin-security check) and #3 (T-13 sentence misparse) → `fde6d43`. #2 → `fde6d43` (banner reworded) + the review-records commit in this PR (outstanding #42/#43 records committed to `docs/reviews/resolved/`; the resolved #44/#64 records moved there for consistency). Companion review: `2026-07-09-pr66-codex-review.md` (2 additional valid findings, fixed in `a5ee69d`; its #2 duplicates #2 here).
- **Reviewed:** 2026-07-09
- **PR:** #66 `fix/step9.4-lift-gate` → `dev` — 3 files, +22/−11, docs-only
- **Method:** adversarial self-review (author == reviewer). Every factual claim in the diff was checked against the repo state on `dev` (committed review records via `git ls-tree`, boundary labels in THREAT_MODEL §3, CI jobs in `ci.yml`, frontend `package.json` + `eslint.config.js`), plus a sweep for stale references to the removed CLAUDE.md heading.
- **CI:** all 6 checks green on `584ee8d`.

## Overview
Replaces CLAUDE.md's temporary Threat Model Gate with the standing rule it prescribed, marks PLAN.md Step 9 COMPLETE with per-substep dispositions, adds a *Threat model* group to the Security Review Process manual checklist, and retitles THREAT_MODEL §8 from "preview" to standing practice. The rule content itself is faithful: a side-by-side check confirms the replacement preserves every element the temporary section's own "replace with" instruction demanded (written/reviewed against the model, impact section with the two allowed forms, never-lags-the-code, missing/wrong blocks merge), and adds the §8 invariants verbatim-in-spirit. B1–B5 / T-nn labels referenced by the new text exist in THREAT_MODEL §3/§6. No other file references the removed "Threat Model Gate" heading. The gitleaks currency fix matches `ci.yml`. The PR body carries both required sections and its "Threat model impact: None" is consistent with the diff (register untouched; only §8's title/intro changed).

## Findings

### 1. [x] Checklist still lists an automated check that does not exist — eslint-plugin-security
`PLAN.md` (Security Review Process, "Automated checks": "**SAST**: eslint-plugin-security (frontend) for DOM XSS, eval usage, etc.")

Verified: `eslint-plugin-security` appears in neither `frontend/package.json` nor `frontend/eslint.config.js` — no frontend SAST job exists in CI. The line is pre-existing, but this PR edits the line directly above it and certifies the whole section as the wired standing practice ("All conditions met"); shipping the certification while the section still advertises a nonexistent CI check is the exact docs-misstate-code failure the gate was built to prevent.

**Fix:** remove the bullet or mark it *(planned — not wired)*, optionally noting what does exist (ruff `S`/bandit cover Python; the frontend relies on TS strict + eslint core + the T-14 CSP, no dedicated SAST).

### 2. [x] "Review records live in `docs/reviews/resolved/`" overstates what is committed
`PLAN.md` (new COMPLETE banner)

Two inaccuracies, verified via `git ls-tree dev -- docs/reviews/`: (a) the #44 and #64 review records live in `docs/reviews/` proper, not `resolved/`; (b) the #42 (threat-model) and #43 (host-validation) review records are **not committed at all** — they exist only as untracked files in a local worktree. That also weakens the banner's "All conditions met" against 9.3's own "Audit report committed under `docs/reviews/`" clause.

**Fix:** reword to "Review records live under `docs/reviews/` (resolved findings archived in `resolved/`)", and commit the two untracked review records (`2026-07-05-pr42-threat-model.md`, `2026-07-06-pr43-host-validation.md`) — ideally in this PR so the banner is true at merge time. They are another session's working files, so confirm with Kristov before sweeping them in.

### 3. [x] Reworded T-13 sentence misparses as "lockfile done"
`PLAN.md` (9.3 cooldown bullet): "Remaining T-13 item: commit a backend lockfile (`uv.lock`) — done as of PR #65: `npm audit` runs in CI…"

The em-dash attaches "done as of PR #65" to the lockfile item, which is exactly wrong — the lockfile is the one item *not* done. The intended meaning (the other two sub-items landed in #65) only emerges on a second read.

**Fix:** split the sentence: "Remaining T-13 item: commit a backend lockfile (`uv.lock`). The other two sub-items landed in PR #65: `npm audit` runs in CI *before* `npm ci` (lockfile-only, audit-before-install) and all Actions are SHA-pinned."

## Nits (non-blocking)
- CLAUDE.md's new heading hard-codes "gate lifted 2026-07-09" — accurate only if merged today; matches the repo's stamp-at-authoring convention, so acceptable, but worth knowing if the merge slips.
- PLAN 9.3's scope line still claims the audit covered "docker-compose", which does not exist (issue #56). Pre-existing and outside this diff's hunks; the v0.1.0 milestone already tracks it.

## Recommendation
Findings 1–3 are all small doc-wording/record-keeping fixes, but 1 and 2 sit squarely in the "docs must not misstate reality" class this PR exists to institutionalize — land them in this PR before merge. Finding 2's file-commit half needs Kristov's OK (files authored by another session).
